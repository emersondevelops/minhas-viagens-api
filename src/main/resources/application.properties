# Altera a porta padrão para a 9090
# server.port=9090

# Deixa o console mais bonitinho
spring.output.ansi.enabled=always

# Configurações do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/minhasviagens?createDatabaseIfNotExist=true
spring.datasource.username=user
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update

# Exibem no console os comandos SQL enviados pelo Spring
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true